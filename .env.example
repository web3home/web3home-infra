# Template — copy to .env and fill in real values
# .env is gitignored and must NEVER be committed

# Domain
PRIMARY_DOMAIN=example.com

# Network
PRIMARY_NODE_IP=192.168.1.10
HEADSCALE_URL=https://hs.example.com

# Add more variables as services come online
