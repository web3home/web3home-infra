[Unit]
# systemd has no native mount retry (upstream RFE #4468, never implemented) and
# automount fails permanently on a failed mount (#16811). OnFailure= -> a retry
# helper is the maintainer-recommended pattern. See JOURNAL 2026-07-13.
OnFailure=cephfs-mount-retry.service
